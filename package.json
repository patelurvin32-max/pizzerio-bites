{
  "name": "pizzerio-bites",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "lint": "npm run lint --prefix client",
    "db:import": "node server/scripts/mongo-migrate.mjs import",
    "db:status": "node server/scripts/mongo-migrate.mjs status"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "csrf-csrf": "^4.0.3"
  }
}
